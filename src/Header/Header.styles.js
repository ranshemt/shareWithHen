import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  headerPadding: {
    paddingLeft: 15,
    paddingTop: 5
  },
  headerText: {
    fontSize: 25,
    color: '#ffffff'
  },
  description: {
    fontSize: 15,
    color: '#ffffff'
  }
})
export default styles
